[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "q_utils"
version = "0.1.0"
description = "My custom quantum learning workspace"
dependencies = []

[tool.setuptools]
packages = ["q_utils"]